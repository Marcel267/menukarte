{% extends 'base.html.twig' %}

{% block title %}New Liste{% endblock %}

{% block body %}
<br>
<div class="container">
    <h2>Anlegen</h2>
    <br>

    {{ include('liste/_form.html.twig') }}

    <a href="{{ path('app_liste_index') }}" class="btn btn-outline-danger btn-sm">Zurück</a>
</div>
{% endblock %}